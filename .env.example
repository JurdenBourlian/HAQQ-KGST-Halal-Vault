# ─────────────────────────────────────────────────────────────────────────────
# HAQQ-KGST Halal Vault — Environment Variables
# Copy to .env and fill in real values. Never commit .env to git.
# ─────────────────────────────────────────────────────────────────────────────

# ── Deployer ──────────────────────────────────────────────────────────────────
PRIVATE_KEY=0x...

# ── Network ───────────────────────────────────────────────────────────────────
# Testnet (current): HAQQ testedge2
RPC_URL_TESTNET=https://rpc.eth.testedge2.haqq.network
CHAIN_ID_TESTNET=54211

# Mainnet: Ethiq L2 (target deployment)
RPC_URL=https://rpc.ethiq.network
CHAIN_ID=30303

# ── Founder address ───────────────────────────────────────────────────────────
# CRITICAL: Receives 20% of ALL protocol fees FOREVER. Immutable after deploy.
# Back up the private key for this address securely.
FOUNDER_ADDRESS=0x...

# ── Token addresses ───────────────────────────────────────────────────────────
# KGST — Kyrgyz Digital Som (bridged to Ethiq L2 via Axelar)
KGST_TOKEN_ADDRESS=0x...

# DEEN — Islamic Gold (1 DEEN = 1g gold, Brinks Dubai, fatwa issued)
# HAQQ L1: 0x4FEBDDe47Ab9a76200e57eFcC80b212a07b3e6cE
# Ethiq L2: TBD — bridge in progress
DEEN_TOKEN_ADDRESS=0x...

# PAXG — Paxos Gold, London vaults. FEE-ON-TRANSFER token (0.02% per tx).
# Ethereum mainnet: 0x45804880De22913dAFE09f4980848ECE6EcbAf78
# PAXG is natively on Ethereum — bringing it to HAQQ L2 requires a dedicated
# bridge and formal agreement with Paxos for multi-chain support.
PAXG_TOKEN_ADDRESS=0x...

# USDC — optional stable pair
USDC_TOKEN_ADDRESS=0x...

# ── Multisig addresses ────────────────────────────────────────────────────────
ADMIN_MULTISIG=0x...       # 5-of-7 — DEFAULT_ADMIN_ROLE (highest privilege)
MANAGER_MULTISIG=0x...     # 3-of-5 — MANAGER_ROLE (operational)
GUARDIAN_MULTISIG=0x...    # 3-of-5 — GUARDIAN_ROLE (emergency pause)

# ── Fee recipients (must sum to 8000 bps — founder's 2000 bps already reserved)
TEAM_MULTISIG=0x...        # 2500 bps (~20% of total fees)
PROTOCOL_FUND=0x...        # 2500 bps (~20% of total fees)
SHARIAH_BOARD=0x...        # 1500 bps (~12% of total fees) — HAQQ Shariah Advisory Board
CHARITY_ADDRESS=0x...      # 1000 bps (~8% of total fees)  — Zakat
DAO_RESERVE=0x...          #  500 bps (~4% of total fees)  — Future DAO

# ── DEX (set after DeployDEX.s.sol) ──────────────────────────────────────────
UNISWAP_V2_FACTORY_ADDRESS=0x...
UNISWAP_V2_ROUTER_ADDRESS=0x...
WETH_ADDRESS=0x...

# ── Pairs (set after DeployDEX.s.sol creates pools) ──────────────────────────
KGST_DEEN_PAIR_ADDRESS=0x...   # Primary: Islamic gold (Mode B default)
KGST_PAXG_PAIR_ADDRESS=0x...   # Secondary: London gold, fee-on-transfer (Modes C/D)
KGST_USDC_PAIR_ADDRESS=0x...   # Tertiary: stable (optional)

# ── Deployed contracts (set after Deploy.s.sol) ───────────────────────────────
VAULT_ADDRESS=0x...
ROUTER_ADDRESS=0x...
FEE_DISTRIBUTOR_ADDRESS=0x...
SHARIAH_GUARD_ADDRESS=0x...

# ── Axelar bridge ─────────────────────────────────────────────────────────────
# Source: BNB Chain (planned). See docs/BRIDGE.md for full payload schema.
AXELAR_GATEWAY=0x...
AXELAR_GAS_SERVICE=0x...
AXELAR_SOURCE_CHAIN=binance
AXELAR_SOURCE_ADDRESS=0x...    # KGST contract on BNB Chain

# ── Explorer ──────────────────────────────────────────────────────────────────
# Testnet
EXPLORER_URL_TESTNET=https://explorer.testedge2.haqq.network
# Mainnet
EXPLORER_URL=https://explorer.ethiq.network
ETHERSCAN_API_KEY=your_api_key
